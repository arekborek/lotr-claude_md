# lotr-claude_md
**One prompt to rule them all** — a reusable configuration library for Claude Code. Paste one master prompt into any project and Claude will pull this library from GitHub and interview you, area by area, to build (or upgrade) that project's `CLAUDE.md`.
